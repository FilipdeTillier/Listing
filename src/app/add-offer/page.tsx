import React, { FC } from "react";

type PageContactProps = {
  className?: string;
};

const PageContact: FC<PageContactProps> = ({}) => {
  return <div className={`nc-PageContact overflow-hidden`}>here</div>;
};

export default PageContact;
